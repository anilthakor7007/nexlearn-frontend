# Backend API URL
# For local development (default if not set)
# NEXT_PUBLIC_BACKEND_URL=http://localhost:5000

# For production (set this in Vercel environment variables)
NEXT_PUBLIC_BACKEND_URL=https://nexlearn-backend.onrender.com
